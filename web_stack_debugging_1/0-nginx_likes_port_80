#!/usr/bin/env bash
# Install Nginx and configure it to listen on port 80

apt-get update
apt-get install -y nginx
service nginx start

# Small delay to ensure Nginx is fully started
sleep 2

# Verify Nginx is actually serving on port 80
curl -s http://localhost:80 > /dev/null

exit 2
